@model BasicMVC.Models.Member
@{
    ViewData["Title"] = "สมาชิก";
}

@*<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>*@

@*<div>
    <p>
        รหัส : @Model.ID<br/>
        ชื่อ-สกุล : @Model.FullName<br/>
        ที่อยู่ : @Model.Address
    </p>
</div>*@

@*<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>รหัส</th>
                <th>ชื่อ-สกุล</th>
                <th>ที่อยู่</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ID</td>
                    <td>@item.FullName</td>
                    <td>@item.Address</td>
                </tr>
            }
        </tbody>
    </table>
</div>*@

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <form asp-controller="Home" asp-action="Index">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="FullName" calss="control-label">ชื่อ-สกุล : </label>
                    <input type="text" asp-for="FullName" class="form-control"/>
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Address" calss="control-label">ที่อยู่ : </label>
                    <input type="text" asp-for="Address" class="form-control"/>
                    <span asp-validation-for="Address" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </form>
        </div>
    </div>
</div>